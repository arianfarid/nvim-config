# Arian Farid's nvim configuration

## Project Structure

```bash
├── init.lua
└── lua
    ├── config
    │   ├── init.lua -- Register custom configurations here.
    │   ├── lazy.lua -- Bootstrap lazy.nvim manager.
    │   ├── remap.lua -- Custom keymaps.
    │   └── set.lua -- Custom nvim settings.
    └── plugins
        └── ... -- Plugins registered here!
```
